import styled from 'styled-components';

const Pill = styled.div`
	background: rgb(var(--semantic-color-info));
	color: white;
	border-radius: 4px;
	font-size: 10px;
	font-weight: 600;
	line-height: 16px;
	padding: 0 4px;
	display: flex;
	align-items: center;
`;

export default Pill;
